// steps/matrix-step.component.ts
import { Component } from '@angular/core';

@Component({
  standalone: true,
  selector: 'app-matrix-step',
  template: `
    <h3>Dependency Matrix (placeholder)</h3>
    <p>We’ll render the matrix here after Checklist → Graph.</p>
  `,
})
export class MatrixStepComponent {}
